# Database
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=root
DB_NAME=maznet
DB_PORT=3306

# Server
PORT=3001

# Encryption (AES-256-GCM for ai_api_key & mikrotik password)
# Generate a 32-byte hex key with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# WARNING: changing this key invalidates all encrypted data in the database.
ENCRYPTION_KEY=
